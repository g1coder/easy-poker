/** @type {import('prettier').Config} */
const config = {
    endOfLine: 'lf',
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: 'es5',
    printWidth: 120,
};

export default config;
